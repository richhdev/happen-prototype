import Hosts from "./Hosts";

export default {
  title: "Sections/Hosts",
  component: Hosts,
  tags: ["!autodocs"],
  parameters: { layout: "fullscreen", backgrounds: { disable: true } },
};

export const Default = {};
